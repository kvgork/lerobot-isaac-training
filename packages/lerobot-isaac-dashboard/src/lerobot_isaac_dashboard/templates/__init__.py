"""templates — Jinja2 templates for the static HTML report.

This package exists so ``importlib.resources`` can locate the bundled
``.j2`` template files when the package is installed as a wheel.
"""
