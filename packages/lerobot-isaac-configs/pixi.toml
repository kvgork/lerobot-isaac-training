# packages/lerobot-isaac-configs/pixi.toml
# DORMANT in monorepo mode — only active after spinout via git subtree split.
# In monorepo mode the root pixi.toml governs all environments.

[workspace]
name = "lerobot-isaac-configs"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.10,<3.12"
pip = "*"

[pypi-dependencies]
lerobot-isaac-configs = { path = ".", editable = true }

[feature.dev.dependencies]
pytest = "*"
hatchling = "*"

[environments]
default = ["dev"]
