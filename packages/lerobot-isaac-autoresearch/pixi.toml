# packages/lerobot-isaac-autoresearch/pixi.toml
# DORMANT in monorepo mode — only active after spinout via git subtree split.
# In monorepo mode the root pixi.toml governs all environments.

[workspace]
name = "lerobot-isaac-autoresearch"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.10,<3.12"
pip = "*"

[pypi-dependencies]
lerobot-isaac-autoresearch = { path = ".", editable = true }

[feature.dev.dependencies]
pytest = "*"
hatchling = "*"

[environments]
default = ["dev"]
